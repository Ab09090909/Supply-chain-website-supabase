# AI Supply Chain Platform - Python Dependencies
# NOTE: The `supabase` package is OPTIONAL — the app falls back to
# `database/supabase_lite.py` (pure requests) if it's not installed.

streamlit==1.39.0
python-dotenv==1.0.1
requests>=2.31.0
pandas==2.2.3
numpy==1.26.4
scikit-learn==1.5.2
Pillow==10.4.0
pydantic==2.9.2
email-validator==2.2.0
groq==0.11.0
