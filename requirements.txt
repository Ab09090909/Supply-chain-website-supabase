# AI Supply Chain Platform - MINIMAL requirements for Streamlit Cloud
# Only the essentials. Everything else is optional (app degrades gracefully).

streamlit>=1.39.0
requests>=2.31.0
python-dotenv>=1.0.1
supabase
